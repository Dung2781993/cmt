
@(Html.Kendo().DropDownList()
    .Name("CampaignTypeId") // Name of the widget should be the same as the name of the property
    .DataValueField("CampaignTypeId") // The value of the dropdown is taken from the EmployeeID property
    .DataTextField("CampaignTypeName") // The text of the items is taken from the EmployeeName property
    //.BindTo((System.Collections.IEnumerable)ViewData["campaignTypes"]) // A list of all employees which is populated in the controller
    .DataSource(source => source.Read(read => read.Action("CampaignTypes_Read", "Campaign")))
)